@model OffTheCuff.Models.Student

@using (var form = Html.BeginForm("Index", "Home", FormMethod.Post))
{
    @Html.EditorFor(x => x.Name)
    @Html.EditorFor(x => x.ID)
    <input type="submit" value="Stuff" />
}
