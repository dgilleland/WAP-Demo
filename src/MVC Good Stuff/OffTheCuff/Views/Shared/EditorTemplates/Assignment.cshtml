@model OffTheCuff.Models.Assignment

@using (var form = Html.BeginForm("MakeWork", "Home", FormMethod.Post))
{
    @Html.EditorFor(x => x.Name)
    @Html.EditorFor(x => x.Weight)
    <input type="submit" value="WORK WORK WORK" />
}


